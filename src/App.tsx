import React from 'react';
import {SafeAreaView} from 'react-native';
import Timer from './pages/Timer';

const App = () => {
  return (
    <SafeAreaView>
      <Timer />
    </SafeAreaView>
  );
};

export default App;
